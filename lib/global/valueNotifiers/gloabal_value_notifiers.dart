

import 'package:flutter/cupertino.dart';

class GlobalValueNotifiers {

  // Notification
  static ValueNotifier<bool> isGetNotify = ValueNotifier<bool>(false);
  static ValueNotifier<bool> isBackupEnable = ValueNotifier<bool>(false);

  // Enable Backup
}